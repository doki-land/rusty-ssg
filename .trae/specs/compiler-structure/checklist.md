# 编译器项目结构整理 - 验证清单

## 目录结构验证
- [ ] 所有编译器项目都包含 `bin` 目录，且目录下只有一个主入口文件 `compiler-name.rs`
- [ ] 所有编译器项目都包含 `src` 目录，且目录结构符合统一规范
- [ ] 所有编译器项目的 `src` 目录下都包含 `compiler`、`config`、`watcher`、`data`、`errors`、`plugin`、`tools`、`types` 等子目录
- [ ] 所有编译器项目的 `compiler` 目录下都包含 `parser` 和 `renderer` 子目录
- [ ] 所有编译器项目的 `tools` 目录下都包含 `cmd` 子目录
- [ ] 所有编译器项目都包含 `tests` 目录，用于存放测试代码
- [ ] 所有编译器项目都包含 `templates` 目录，用于存放模板文件

## 代码完整性验证
- [ ] 所有编译器项目都能够正常编译
- [ ] 所有代码文件都被正确移动到新的目录结构中，没有丢失或损坏
- [ ] 所有导入路径都已更新，确保代码能够正常编译
- [ ] 所有测试都能够正常运行

## 文档注释验证
- [ ] 所有公共结构体都有文档注释
- [ ] 所有公共枚举都有文档注释
- [ ] 所有公共方法都有文档注释
- [ ] 所有公共字段都有文档注释
- [ ] 没有使用后置注释

## 代码风格验证
- [ ] 遵循 Rust 官方代码风格
- [ ] 使用 rustfmt 格式化代码
- [ ] 代码简洁明了

## 最终验证
- [ ] 所有编译器项目都按照统一规范组织目录结构
- [ ] 所有项目能够正常编译
- [ ] 所有测试能够正常运行
- [ ] 文档注释完整
- [ ] 代码风格一致